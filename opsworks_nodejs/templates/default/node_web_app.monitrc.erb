check process node_web_app_<%= @application_name %> with pidfile /var/run/<%= @application_name %>.pid
  start program = "/bin/sh -c 'cd <%= @deploy[:deploy_to] %>/current; /bin/rm /var/run/<%= @application_name %>.pid; /usr/bin/env NODE_PATH=<%= @deploy[:deploy_to] %>/current/node_modules:<%= @deploy[:deploy_to] %>/current <% (node[:environment_variables] || {}).merge(@deploy[:environment_variables] || {}).each do |name,value| -%><%= name %>=<%= value %> <% end -%> /usr/local/bin/npm start >> log/server.log'"
  stop program  = "/bin/cat /var/run/<%= @application_name %>.pid | /usr/bin/xargs /bin/kill; /bin/rm /var/run/<%= @application_name %>.pid"
